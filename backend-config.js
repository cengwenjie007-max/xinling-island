window.XINLING_BACKEND_CONFIG = {
  // Supabase 项目 URL。这里只能填写公开项目地址。
  supabaseUrl: "",
  // Supabase anon public key。不要把 service role key 放到前端。
  supabaseAnonKey: "",
  // Cloudflare Worker 地址，例如 https://your-api.yourname.workers.dev
  apiBaseUrl: ""
};
